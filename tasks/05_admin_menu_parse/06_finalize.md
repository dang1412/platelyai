# 06 — Hoàn tất

## Việc
- Chạy trong `app/`: `pnpm lint && pnpm test && pnpm build` — phải xanh.
- Self-check AGENTS §9: validate-at-the-edge ở cả 2 route; SQL tham số hoá; không raw hex;
  Gemini gọi 1 lần/parse; file không vượt 300 LOC chưa tách concern.
- Cập nhật `tasks/05_admin_menu_parse/00_overview.md` (tick hết).
- Commit theo conventional commits (`feat: admin parse menu image to DB`), **không** ref AI.
- Mở PR từ `feat/admin-menu-parse` → `main`.

## Done khi
- CI/local xanh, PR mở.
