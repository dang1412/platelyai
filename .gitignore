# Secrets — chỉ commit .env.example
.env
.env.*
!.env.example

# Dependencies (app/ có .gitignore riêng; chặn luôn node_modules ở root)
/node_modules

# Symlink tham khảo code cũ (local-only, trỏ ra ngoài repo)
/old

# DB backup dumps (local-only)
/backups

# OS / editor
.DS_Store
*.swp
